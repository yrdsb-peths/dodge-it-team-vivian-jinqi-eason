import greenfoot.*;
    private class spawnCoin { 
        Coin coin = new Coin();
        int x = Greenfoot.getRandomNumber(getWidth());  
        int y = Greenfoot.getRandomNumber(getHeight()); 
        //summon object
    }
    public class respawnCoin {
        //spawn coin
    }
        if(coin.isTouching bee) {
        //hide coin
    }
}